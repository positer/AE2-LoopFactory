# 2026-09-13 README rewrite

Replaced the accumulated development-log README with a long-lived, version-independent project description for AE2-LoopFactory (AE2LF).

The new README now covers:

- project identity and stable product scope without embedding a release number, local deployment path, or development-build status;
- Loop Factory blocks, terminal execution refresh rules, code language, channels, comments, highlighting, and saved state;
- compressed bulk logistics, checked signed-64-bit transfer accounting, per-job backpressure/recovery state, and the limits of measured validation;
- Crafting Ripper, Loop Card, Recipe Ring Solver Terminal, and Supercomputing Crafting Optimizer Interface;
- Loop Storage Cell tiers, portable cells, recipe counts, optional integrations, and capability boundaries;
- version-selection and installation rules without hard-coding a compatibility matrix;
- stable documentation and development-layout links;
- unloaded-chunk behavior and overlapping physical-tag boundaries without embedding a single saved-world diagnosis.

No source code, build artifact, PCL instance, save, dependency, or release metadata was changed by this documentation pass. The README links to the existing dated verification reports rather than duplicating their full logs.

The README keeps dated measurements, failed attempts, hashes, release metadata, local PCL details, and raw observations in archive reports. It explicitly limits performance language to measured workloads and does not promise unlimited TPS or universal third-party-machine compatibility.
